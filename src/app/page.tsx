import { Button } from "@/components/ui/button";
import { Ghost } from "lucide-react";

const x = ()=>{
    return (
        <div>
          <Button>
            click me
          </Button>
        </div>
    );
};

export default x;